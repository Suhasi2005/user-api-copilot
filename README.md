# User Management API

A REST API for managing user records with full CRUD support (Create, Read, Update, Delete).

## Features
- 5 endpoints: create, list, get by ID, update, delete
- Request logging middleware
- Input validation on user data

## Tech Stack
Node.js, Express

## Run locally
```bash
npm install
npm start
```
